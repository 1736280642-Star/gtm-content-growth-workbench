# V5 新版用户流程图：月度内容矩阵

## 流程定位

V5 主流程不是周计划，而是：

```text
月度目标输入
-> 月度内容策略包
-> 月度内容矩阵
-> 人工审核
-> 批量生成一个月内容
-> 自动质检
-> 异常队列
-> 定时自动化发布
-> 月度复盘
```

周计划 / 日任务只是月度矩阵拆解后的执行视图。

## 总流程图

```mermaid
flowchart TD
  A["产品或资料进入工作台"] --> B{"是否已有产品实体"}
  B -- "没有" --> C["创建产品实体\n品牌 / 官网 / owner / 默认渠道"]
  B -- "已有" --> D["选择产品实体"]
  C --> D

  D --> E["导入知识库资料\n官网 / 文档 / FAQ / 案例 / 技术资料"]
  E --> F["资料解析与切片"]
  F --> G["资料分层与元数据标注\n权威等级 / 场景 / 能力 / 用户"]
  G --> H["产品事实抽取"]
  H --> I["生成产品表达规则包草稿"]
  I --> J{"人工确认规则包"}

  J -- "确认生效" --> K["规则包 active"]
  J -- "缺资料" --> L["资料缺口队列"]
  J -- "放弃或回滚" --> M["规则包归档 / 回滚"]
  L --> E
  M --> I

  K --> N["进入月度内容生产池"]
  N --> O["月度目标输入\n产品配额 / 渠道比例 / 发布频率"]
  O --> P["Workflow Agent 生成月度内容策略包"]
  P --> Q["规则引擎校验策略包"]
  Q --> R{"人工审核月度策略"}
  R -- "退回调整" --> O
  R -- "通过" --> S["生成月度内容矩阵\n标题 / 日期 / 渠道 / 产品 / 蒸馏词"]

  S --> T["矩阵项生成 EvidencePack"]
  T --> U{"矩阵是否可批量生成"}
  U -- "缺规则包 / 缺官网 / 缺证据" --> V["矩阵异常队列"]
  V --> E
  U -- "可生成" --> W["批量生成一个月正文"]

  W --> X["规则引擎硬校验"]
  X --> Y["评测 Runner 自动质检"]
  Y --> Z{"自动分流"}
  Z -- "合格稿" --> AA["进入定时发布排程"]
  Z -- "异常稿" --> AB["人工异常队列"]

  AB --> AC["人工处理\n补资料 / 改写 / 保留原因 / 转 Badcase"]
  AC --> AD["Badcase 归因"]
  AD --> AE{"反推动作"}
  AE -- "补知识库" --> E
  AE -- "补规则包" --> I
  AE -- "补生成规则" --> AF["Prompt / 生成标准更新"]
  AE -- "补评测项" --> AG["评测集更新"]

  AA --> AH["人工抽检 / 发布前确认"]
  AH --> AI["定时自动化发布"]
  AI --> AJ{"发布结果"}
  AJ -- "成功" --> AK["URL 自动回填或人工确认"]
  AJ -- "失败" --> AL["发布失败队列\n重试 / 人工接管"]
  AL --> AH
  AK --> AM["数据回传"]
  AM --> AN["月度复盘"]
  AN --> AO["下月策略优化建议"]
  AO --> O
```

## 页面级流程图

```mermaid
flowchart LR
  subgraph Foundation["底座治理"]
    S1["设置\n产品实体 / 月度配额 / 发布频率"]
    K1["知识库导入\n资料类型 / 权威等级 / 产品归属"]
    K2["知识库详情\n事实抽取 / chunk 元数据 / 缺口"]
    R1["规则包页\n允许表达 / 禁止表达 / 证据要求"]
    T1["蒸馏词池\n主题种子 / GEO缺口 / Badcase来源"]
  end

  subgraph Month["月度计划与生产"]
    M1["月度内容矩阵\n月度目标 / 策略包 / 矩阵审核"]
    B1["批量生成中心\nEvidencePack / 正文生成 / 自动质检"]
    E1["异常队列\n证据不足 / 规则越界 / 渠道错配"]
    D1["草稿抽检\n异常稿处理 / 合格稿抽检"]
  end

  subgraph Publish["自动发布与复盘"]
    P1["发布排程\n定时发布 / 重试 / 人工接管"]
    C1["当日执行\n今日排程 / 发布状态 / URL确认"]
    M2["月度复盘\n矩阵完成率 / 发布成功率 / 下月建议"]
    A1["AI治理\nAgent / RAG / Rule / Eval / Publish日志"]
  end

  S1 --> K1 --> K2 --> R1 --> T1 --> M1
  M1 --> B1 --> E1
  B1 --> D1 --> P1 --> C1 --> M2
  E1 --> K1
  E1 --> R1
  E1 --> D1
  M2 --> M1
  B1 --> A1
  P1 --> A1
```

## 角色流程图

```mermaid
flowchart TD
  A["知识库维护者"] --> A1["导入产品资料"]
  A1 --> A2["检查解析 / 切片 / 权威等级"]
  A2 --> A3["生成规则包草稿"]
  A3 --> A4["提交人工确认"]

  B["工作台运营者"] --> B1["确认规则包"]
  B1 --> B2["维护月度生产池"]
  B2 --> B3["处理异常队列"]
  B3 --> B4["查看 Agent / RAG / Rule / Publish 日志"]

  C["内容增长负责人"] --> C1["输入月度目标"]
  C1 --> C2["审核月度策略包"]
  C2 --> C3["确认月度内容矩阵"]
  C3 --> C4["查看月度复盘与下月建议"]

  D["内容发布人员"] --> D1["抽检合格稿"]
  D1 --> D2["处理异常稿"]
  D2 --> D3["查看发布排程"]
  D3 --> D4["处理发布失败 / URL确认"]

  E["开发管理员"] --> E1["真实接入诊断"]
  E1 --> E2["Provider / Embedding / RAG / Agent / Publish 状态"]
  E2 --> E3["排查配置缺失与发布失败"]
```

## 新流程中的关键保护点

1. 规则包草稿不能自动生效。
2. 没有 active 规则包的产品不能进入月度内容生产池。
3. 月度矩阵必须人工审核后才能批量生成。
4. 没有 EvidencePack 的矩阵项不能生成正文。
5. 规则引擎 blocker 必须进入异常队列。
6. 合格稿进入定时发布前必须通过自动质检，并支持人工抽检。
7. 定时正式自动发布依赖真实平台配置；缺配置时显示 `pending_config`。
8. 发布失败必须进入失败队列，不能伪装为已发布。

